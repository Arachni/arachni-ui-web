<% title @dispatcher.url %>
<% breadcrumb_add 'Dispatchers', dispatchers_path %>

<div id="dispatchers">
    <div id="dispatcher"
         data-refresh-url="<%= show_partial_dispatcher_url( @dispatcher ) %>"
         data-refresh-rate="<%= Settings.dispatcher_refresh_rate %>"
    >
        <%= render @dispatcher %>
    </div>
</div>
