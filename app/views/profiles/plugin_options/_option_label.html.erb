<label for="profile[plugins][<%= plugin_name %>][<%= option.name %>]" >
    <% description = (option.description.ends_with?( '.' ) ?
            option.description[0...-1] : option.description)  %>
    <% if option.required? %>
        <abbr title="required">*</abbr>
    <%end%>

    <%= description %> (<strong><em><%= option.name %></em></strong>):
</label>
