
<% if !@scan.description.to_s.empty? %>
<blockquote>
    <%= @scan.description %>
</blockquote>

<hr/>
<% end %>

<%=  render partial: 'comment_list', locals: { scan: @scan } %>

<%= render @scan.active? ? 'show_active' : 'show_inactive' %>
