$("#table").data( "refresh-url", "<%= escape_javascript schedule_scans_url( format: :js, params: params ) %>" );
$("#table").html( "<%= escape_javascript( render( 'schedule_table.html' ).force_encoding( 'utf-8' ) ) %>" );

$("#scan-groups-list").html( "<%= escape_javascript render( partial: 'scan_groups/list_with_tabs.html', locals: { path_method: method( :schedule_scans_url ) } ) %>" );

window.setupScanGroupHooks();

<% if @group %>
    $("#selected_group_id").val( <%= @group.id %> );
<% end %>
